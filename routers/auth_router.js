import express from "express";
const router = express.Router();
//import { authController } from "../client/controller/auth_controller.js";

//router
//  .route("/auth/signUp")
//  .post(authController.signUpUserWithEmailandPassword);
//router
//  .route("/auth/signIn")
//  .post(authController.signInUserWithEmailAndPassword);
//router.route("/auth/signOut").get(authController.signOutUser);
//router.route("/auth/checkState").get(authController.checkUserAuthState);

export const authRouter = router;
